repositories:
- name: stable
  url: https://kubernetes-charts.storage.googleapis.com

releases:
- name: traefik-public
  namespace: ingress
  chart: stable/traefik
  version: 1.86.2
  values:
    - values.yaml
